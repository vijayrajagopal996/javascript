/**
 * http://usejsdoc.org/
 */

//includes are addded on strings and arrays
//node --harmony_array_includes
const numbers = [1, 2, 3, 4, 5];
//console.log(numbers.include(3));

// exponential operator
const square = 2 ** 2;
console.log(square);

const cube = 2 ** 3;
console.log(cube);